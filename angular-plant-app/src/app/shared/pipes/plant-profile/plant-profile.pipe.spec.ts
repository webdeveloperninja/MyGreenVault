import { PlantProfilePipe } from './plant-profile.pipe';

describe('PlantProfilePipe', () => {
  it('create an instance', () => {
    const pipe = new PlantProfilePipe();
    expect(pipe).toBeTruthy();
  });
});
